/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vercel uses its own output; standalone is for self-hosted Node server (e.g. Docker).
  // output: "standalone",
};

module.exports = nextConfig;
